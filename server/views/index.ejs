<%
  const total = todos.length;
  const done = todos.filter((t) => t.done).length;
  const pending = total - done;
%>

<section class="space-y-8">
  <!-- Hero -->
  <div class="space-y-3">
    <div class="inline-flex items-center gap-2 rounded-full bg-emerald-50 px-3 py-1 text-xs font-medium text-emerald-600 ring-1 ring-emerald-200">
      <span class="h-1.5 w-1.5 rounded-full bg-emerald-500"></span>
      htmx + Express + Vite + Tailwind CSS
    </div>
    <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl"><%= t('hero.title') %></h2>
    <p class="max-w-xl text-sm leading-relaxed text-gray-500">
      <%- t('hero.description') %>
    </p>
  </div>

  <!-- 概览统计 -->
  <div id="stats" class="grid grid-cols-3 gap-4">
    <div class="card p-4">
      <p class="text-xs text-gray-400"><%= t('todos.all') %></p>
      <p class="mt-1 text-2xl font-bold text-gray-900"><%= total %></p>
    </div>
    <div class="card p-4">
      <p class="text-xs text-gray-400"><%= t('todos.done') %></p>
      <p class="mt-1 text-2xl font-bold text-emerald-600"><%= done %></p>
    </div>
    <div class="card p-4">
      <p class="text-xs text-gray-400"><%= t('todos.pending') %></p>
      <p class="mt-1 text-2xl font-bold text-amber-500"><%= pending %></p>
    </div>
  </div>

  <!-- htmx 表单：POST /todos，用返回的局部片段替换 #todo-list -->
  <div class="card border border-gray-100 p-6">
    <div class="mb-5 flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <h3 class="text-lg font-semibold text-gray-900"><%= t('todos.title') %></h3>
        <p class="text-xs text-gray-400"><%= t('todos.section_hint') %></p>
        <p class="text-xs text-gray-400"><%- t('todos.count', { count: total }) %></p>
      </div>
      <!-- htmx 手动刷新列表 -->
      <button
        class="btn-ghost"
        hx-get="/todos"
        hx-target="#todo-list"
        hx-swap="outerHTML">↻ <%= t('todos.refresh') %></button>
    </div>

    <form
      class="mb-5 flex flex-col gap-2 sm:flex-row sm:gap-3"
      hx-post="/todos"
      hx-target="#todo-list"
      hx-swap="afterbegin"
      hx-on::after-request="if(event.detail.successful) this.reset()">
      <input
        type="text"
        name="text"
        required
        data-required="<%= t('validation.required') %>"
        oninvalid="this.setCustomValidity(this.dataset.requiredMsg)"
        oninput="this.setCustomValidity('')"
        placeholder="<%= t('todos.placeholder') %>"
        class="input sm:flex-1" />
      <button type="submit" class="btn-primary">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
        </svg>
        <%= t('todos.add') %>
      </button>
    </form>

    <div class="rounded-xl border border-gray-100 p-2 sm:p-3">
      <%- include('partials/list', { todos }) %>
    </div>
  </div>
</section>